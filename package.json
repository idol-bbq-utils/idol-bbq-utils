{
    "name": "idol-bbq-utils",
    "version": "0.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "build": "lerna run build",
        "dev:spider": "lerna run --scope=@idol-bbq-utils/spider dev",
        "dev:forwarder": "lerna run --scope=@idol-bbq-utils/tweet-forwarder dev",
        "run:forwarder": "lerna run --scope=@idol-bbq-utils/tweet-forwarder run",
        "format": "pnpm prettier --write . --ignore-path .prettierignore",
        "docs:dev": "lerna run --scope=docs docs:dev",
        "docs:build": "lerna run --scope=docs docs:build",
        "docs:preview": "lerna run --scope=docs docs:preview"
    },
    "keywords": [],
    "author": "",
    "license": "MIT",
    "engines": {
        "node": ">=20.0.0"
    },
    "devDependencies": {
        "@types/bun": "^1.2.8",
        "prettier": "^3.3.2",
        "typescript": "^5.5.3"
    },
    "workspaces": [
        "core/*",
        "app/*",
        "docs/*"
    ]
}
